import styled from 'styled-components';

/* interface LabelProps {
  htmlFor: string;
  name: string;
}

export const StyledLabel = ({ htmlFor, name }: LabelProps) => {
  return <Label htmlFor={htmlFor}>{name}</Label>;
}; */

export const StyledLabel = styled.label`
  /*  padding: 10px 0;
  color: #fff;
  pointer-events: none;
  transition: 0.5s; */
  display: none;
`;
